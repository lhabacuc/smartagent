1. Fazer release 0.2.0 agora.
   As mudancas de API/config ja sao relevantes e com compatibilidade preservada, entao e um bom ponto para versionar.

2. Criar CHANGELOG.md com Migration Guide.
   Incluir:
   - novo recomendado: Agent(provider=..., model=...)
   - novas env vars: SMARTAGENT_*
   - deprecacoes: Agent.run/help/... e model como provider (ainda suportado por enquanto)

3. Adicionar testes de integracoes com mock HTTP.
   Cobrir timeout/retry e parsing de resposta para cada provider (openai, groq, gemini, etc.).

4. Definir politica de deprecacao com versoes.
   Exemplo:
   - 0.2.x: apenas warning
   - 0.3.0: remover model como provider e wrappers CLI legados

5. (Opcional, recomendado) criar CLI oficial (smartagent) separada.
   O modulo agent/cli.py ja existe; transformar em entrypoint oficial.
